# Test wallet fixtures

`wallet.ts` generates a Solana keypair at runtime via solders and seeds
AadhaarChain verified trust through `/api/identity/dev/fixtures/{wallet}`.

Do not commit private keys.
